# .ai/skills

技能是可复用的项目操作模板，适合稳定、重复、带有项目约束的工作流。

## 命名规范

每个技能一个目录，目录名使用 kebab-case，正文固定为 `SKILL.md`。`SKILL.md` 的 YAML frontmatter 中 `name` 必须与目录名一致。

## 创建条件

仅当一个操作在项目中会反复出现、有明确输入输出、步骤稳定、包含项目特有约束且未来可复用时，才创建技能。

