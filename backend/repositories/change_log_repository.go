// 文件说明：仓储层预留，后续隔离 SQL 持久化和服务层业务编排。
// 联动 backend/database、backend/services 和 API 写操作。

package repositories

// Change log repository will record single-cell edits, batch operations, imports, exports and restore events.
